# Markdown 转 HTML

行内公式：$E = mc^2$。

块级公式：

$$
\int_{-\infty}^{\infty} e^{-x^2}\,dx = \sqrt{\pi}
$$

JavaScript 代码：

```javascript
const greet = (name) => `Hello, ${name}!`;
console.log(greet('Markdown'));
```

| 功能 | 状态 |
| --- | --- |
| MathJax | 支持 |
| 代码高亮 | 支持 |
